<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/src/scss/disciplines_scss/Allcourses.css">
    <title>Системный анализ и управление</title>
</head>
<body>
    <header class="header">
        {% block header %}
            {{ include('@relax/header.html.twig') }}
        {% endblock %}
    </header>
    <main class="main">
        <p class="main__title">Мои курсы</p>
        <form action="#" class="main__form">
            <input type="search" class="main__form-item" placeholder="Поиск">
            <a href="#">
                <img class="form__search" src="/src/image/search.svg" alt="">
            </a>
        </form>
        <div class="courses__container">
            {% for item in coursesData %}
                <div class="courses__wrapper">
                    <a href="#" class="courses__href">
                        <div class="courses__block-1">
                            <img class="courses__image" src="/src/image/{{ item.image }}" alt="">
                        </div>
                    </a>
                    <a href="" class="courses__href-2">
                        <div class="courses__block-2">
                            <p class="courses__title">{{ item.title }}</p>
                            <p class="courses__description">{{ item.description }}</p>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </main>
</body>
</html>